import { Link } from '../../atoms/cta/Link';
import { Limit } from '../../atoms/limit/Limit';

export class LinkList {
    lists?: LinkList[];
    links?: Link[];
    limit?: Limit;
    label?: string;
}
